# This is a Node.js full-stack project. Python isn't used; backend dependencies are in package.json.
# No Python dependencies are required for this repository.
# The application is built with Node.js; install backend packages with:
#   cd backend
#   npm install
